{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Agence list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nomagance</th>
                <th>Logo</th>
                <th>Adresse</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('agence_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nomagance }}</td>
                <td>{{ entity.logo }}</td>
                <td>{{ entity.adresse }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('agence_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('agence_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('agence_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
