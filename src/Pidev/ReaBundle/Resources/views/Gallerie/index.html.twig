{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Gallerie list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idimage</th>
                <th>Url</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('gallerie_show', { 'id': entity.idimage }) }}">{{ entity.idimage }}</a></td>
                <td>{{ entity.url }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('gallerie_show', { 'id': entity.idimage }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('gallerie_edit', { 'id': entity.idimage }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('gallerie_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
