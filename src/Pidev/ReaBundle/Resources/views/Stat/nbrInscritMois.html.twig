
{% extends 'PidevReaBundle:Stat/chart:columnStackedAndClustered.html.twig' %}
{% block stat %}
{% set year = "" %}
{% set test = 1 %}
{% for stat in stats %}
     {% if test !=1%}
    {% if year !=stat.year %}
        },{
        "year":{{stat.year}} ,
        "{{stat.mois}}":{{stat.nbr}},
    {% set year = stat.year %}
    {% else %}
     "{{stat.mois}}":{{stat.nbr}},
    {% endif %}
    {% else %}
        {% set test = 2 %}
            {% if year !=stat.year %}
        {
        "year":{{stat.year}} ,
        "{{stat.mois}}":{{stat.nbr}}
    {% set year = stat.year %}
    {% else %}
     "{{stat.mois}}":{{stat.nbr}},
    {% endif %}
    {% endif %}
    {% endfor %}
    }
   {% endblock %}