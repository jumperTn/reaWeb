{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Demandeimmeuble list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Nbretage</th>
                <th>Nbrappartement</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('demandeimmeuble_show', { 'id': entity.iddemande }) }}">{{ entity.nbretage }}</a></td>
                <td>{{ entity.nbrappartement }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('demandeimmeuble_show', { 'id': entity.iddemande }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('demandeimmeuble_edit', { 'id': entity.iddemande }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('demandeimmeuble_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
