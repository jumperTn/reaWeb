{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nouveau</h1>

    <table class="record_properties">
        <tbody>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('nouveau') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('nouveau_edit', { 'id': entity.idoffre }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
